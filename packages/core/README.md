# aistruth-core

Shared Python package for time alignment, AIS adapter interfaces, and fusion helpers.

Install (editable) from repo root:

```bash
pip install -e "packages/core[dev]"
pytest packages/core/tests -q
```
