services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: aistruth
      POSTGRES_PASSWORD: aistruth
      POSTGRES_DB: aistruth
    ports:
      - "5432:5432"
    volumes:
      - ./docker/init-db:/docker-entrypoint-initdb.d:ro
